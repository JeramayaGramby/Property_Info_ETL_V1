# Importing all necessary libraries
import os
import pandas as pd
import requests
import warnings


property_address = str(input(f'Enter the full Property Address with the zip code (Example: 423 Main Street 18015)'))

